# MongoDB connection URI
MONGODB_URI=mongodb://localhost:27017/mern_blog

# JWT secret for auth
JWT_SECRET=change_this_to_a_secure_secret

# Server port
PORT=5000

# Optional: file storage base URL (if using static serving of uploads)
FILES_BASE_URL=http://localhost:5000/uploads